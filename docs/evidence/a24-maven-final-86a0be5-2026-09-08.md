# Final86 Maven packaged-consumer matrix

**Verified on 2026-09-08:** the existing four Maven profiles passed against the
reviewed unsigned local 0.2.0 JARs from source
`86a0be5d2e444f3b73925122fa448d9d1a324edd`. One sequential run completed with exit 0
and full EOF: **24 actual MySQL test executions and 16 negative controls**.

The [machine-readable evidence](a24-maven-final-86a0be5-2026-09-08.json) retains
exact JUnit method names, classfile hashes, per-phase evidence hashes, native
checksum bindings and reviewed artifact identities. Local paths, raw SQL,
connection details and complete native error lines remain private.

| Java / ShardingSphere | Online MySQL | Offline MySQL | Negative controls | Frozen cache entries |
| --- | ---: | ---: | ---: | ---: |
| 17.0.15 / 5.5.2 | 3 passed | 3 passed | 4 passed | 2,453 unchanged |
| 17.0.15 / 5.5.3 | 3 passed | 3 passed | 4 passed | 2,079 unchanged |
| 21.0.11 / 5.5.2 | 3 passed | 3 passed | 4 passed | 2,453 unchanged |
| 21.0.11 / 5.5.3 | 3 passed | 3 passed | 4 passed | 2,079 unchanged |

All 24 executions had zero failures, errors or skips. Each online/offline pair
ran the same three existing fixture methods: exact staged core/provider JAR
origins; unchanged real MySQL capture against its versioned schema-two baseline;
and unchanged business results with expanded physical execution attempts,
assertion failure and both CLI report formats. Those CLI assertions call
`ManifestReviewCli.run` in-process. This matrix does not establish a standalone
CLI process result.

The actual selected ShardingSphere group matched its exact profile and core
resolved transitively from the single selected adapter. The tests retained the
expected business row while hook-reported physical JDBC execution attempts
changed from one to two; the approved baseline remained unchanged, and JSON and
Markdown report assertions returned the expected regression code. Each test JVM
matched its Java profile. Forty compiled classfile occurrences were verified at
major 61 for Java 17 or major 65 for Java 21.

## Fresh and offline consumption

Every online and resolver-negative consumer began with an absent Maven cache.
After each successful online prime, its cache was frozen and inventoried; a
separate consumer executed full offline `clean verify` from a disposable copy.
The original HTTP endpoint was closed first. The exact JDK network probes
established kernel-denied external access while loopback and the local Docker
Unix socket remained available. Both positive phases invoked the fixed
Testcontainers no-pull policy and used the same inspected local MySQL/Ryuk image
identities. All 9,064 frozen cache entries remained unchanged.

The existing repository-side Central response cache reused verified responses
within this one run. It imported no developer Maven cache. Its 2,320 retained
response bodies were independently rehashed; these are repository response
counts, not additional consumed artifacts or tests.

Every profile separately passed all four controls:

- Native strict-checksum rejection of the exact one-byte-corrupted core JAR,
  bound to the reviewed original JAR, unchanged sidecar and completed JAR/sidecar
  GETs with the matching native expected/actual digest pair.
- Rejection of correct reviewed core/adapter bytes downloaded from the actual
  unintended repository identity.
- Native Enforcer rejection of the exact wrong ShardingSphere runtime anchor.
- Native Enforcer rejection of the exact wrong non-anchor while all three
  correct runtime anchors remained selected.

## Final source and evidence binding

Reviewed receipt SHA-256:
`1f4bb21b430a03a44d89e6daddc1fbdede1886250637ef83cecedfd71e350c7e`.
Consumer checkout: `3ad510a0af972231fbd074607936ee52be3cad1f`.
Maven: 3.9.14; platform: macOS 26.4.1 arm64.

| Reviewed runtime JAR | SHA-256 |
| --- | --- |
| `routecontract-core:0.2.0` | `9115e1f6e96bece66d29045e3550fc72aa15a217ee82f3289598a9bc693e32ac` |
| `routecontract-shardingsphere-5.5:0.2.0` | `e48e67b2ce2c5d75d60d6794fe93c34d34697daff810a7b1f9c977095c5a02e5` |
| `routecontract-shardingsphere-5.5.2:0.2.0` | `d5e73f86004fc5a00ebdf3069440d16f8eaf2e033e8b57a7d096ace26630fbe4` |

The final independent audit checked all 56 recorded commands, 128 negative
raw-evidence hashes, 3,380 offline cloned JAR/POM/module payload occurrences,
nine reviewed primary payloads, 90 staging files and 69 production/publication
files. Fourteen executed Maven fixture/helper hashes matched the independently
reviewed prefreeze and unchanged checkout, together with actual retained
per-lane copied inputs. The unchanged runner does not create a separate
executed-harness copy. Maven consumes its POM and the shared fixture source;
the separately recorded shared Gradle build hash `7f465d3...` is contextual.

Receipt, review decision, tool identities, certificate bundle, source and
fixtures remained unchanged. The complete retained logs contained no uncaught
thread/shutdown exception. No matrix retry or additional runtime tests were
performed by the independent audit.

| Retained binding | SHA-256 |
| --- | --- |
| Raw aggregate summary | `230340c7063cb2bd49540038bdfe1b4f55c4d32dfa3e5a96ab6f6b7a6a4b7f40` |
| Independent retained audit | `34527b53575710699f91210679ec8e0812994d345a5b8c8f9f681e70a376a306` |
| Executed Maven runner | `86d0e9fdfb56fde20d82e3c1c5afa79c3a005f4d1baf5347137dca4fa164fc76` |

This completes the finite Maven component: `completeMavenA24Matrix=true`.
`fullA24Complete=false` and `publicConsumption=false` remain. Scope is the
existing synchronous, non-batch PreparedStatement fixture, bounded Maven
Java 17/21 profiles and hook-reported execution attempts. No Gradle Java 21,
arbitrary Docker workload, public release or external adoption is established.
The former `4e06694` success and its preserved failed attempt remain historical;
none of their executions is included in these final86 totals.
